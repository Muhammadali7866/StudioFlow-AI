{
  "name": "@studioflow/agents",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "eslint src test --ext .ts,.mjs",
    "test": "npm run build --workspace=@studioflow/shared && npm run build --workspace=@studioflow/config && npm run build && NODE_ENV=test node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^0.2.0",
    "@studioflow/config": "*",
    "@studioflow/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
